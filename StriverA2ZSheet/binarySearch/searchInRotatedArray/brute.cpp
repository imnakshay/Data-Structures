#include<bits/stdc++.h>
using namespace std;

int position(vector<int> &arr, int target){
    int n = arr.size();
    for(in)
}

int main(){
    vector<int> arr = {4,5,6,7,0,1,2};
    int target = 7;
    cout<<position(arr,target);
}