#include<bits/stdc++.h>
using namespace std;

int main(){
    unordered_set<int> s;
    for(int i = 1;i<=10;i++){
        s.insert(i);
    }
    // s.insert(1);
    // s.clear(); //returns the empty set;
    // s.erase(1);
    // for(auto n : s){//==for(int it = s.begin();i!=end();it++)
    //     cout<<n<<" ";
    // }
    //finding the presence of an element from a set; find returns the iterator of the number else set.end();
    // if(s.find(20)!=s.end()){
    //     cout<<"2 is present the set";
    // }else{
    //     cout<<"number is not present.";
    // }

    
}
